# This is the install.R file
# this will
# (1) Its presence tells the INSTALL utility to create a binary image of the package workspace
# (2) Hold code that needs to be executed each time the package is attached after the image
#     is loaded. Few packages have a need for such code, so this is usually an empty file.
